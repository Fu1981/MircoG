# Supabase — öffentliche Werte (sicher für Frontend)
# Werte findest du im Supabase-Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://<dein-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<dein-anon-key>

# Edge Function Base URL (optional, falls abweichend)
# NEXT_PUBLIC_API_URL=https://<dein-project-ref>.supabase.co/functions/v1
